name: verein_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.5+11

environment:
  sdk: ">=3.4.3 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # --- KORRIGIERTE & KRITISCHE PAKETE ---
  # firebase_core: MINDESTENS ^4.0.0 erforderlich.
  # Alle Firebase-Pakete müssen auf eine konsistente, moderne Version gebracht werden.
  firebase_core: ^4.2.1                  # (War 3.10.1, jetzt auf ^4.2.1)
  firebase_auth: ^6.1.2                   # (War 5.5.0, jetzt auf ^6.1.2)
  firebase_auth_web: ^6.1.0               # (War 5.14.0, jetzt auf ^6.1.0)
  cloud_firestore: ^6.1.0                 # (War 5.6.4, jetzt auf ^6.1.0)
  firebase_storage: ^13.0.4               # (War 12.4.3, jetzt auf ^13.0.4)
  firebase_messaging: ^16.0.4             # (War 15.2.3, jetzt auf ^16.0.4)
  firebase_database: ^12.1.0              # (War 11.3.3, jetzt auf ^12.1.0)
  cloud_functions: ^6.0.4                 # (War 5.4.0, jetzt auf ^6.0.4)

  # Kritisch für den ursprünglichen Fehler
  file_picker: ^10.3.7                    # (Bleibt, da es der Fix war)

  # --- ALLGEMEINE UPDATES ---
  provider: ^6.1.5                        # (War 6.0.3, jetzt auf ^6.1.5)
  shared_preferences: ^2.2.2              # (War 2.1.1, jetzt auf ^2.2.2)
  http: ^1.2.2                            # (Bleibt)
  flutter_local_notifications: ^19.5.0    # (War 18.0.1, jetzt auf ^19.5.0)
  table_calendar: ^3.1.3                  # (Bleibt, 3.2.0 wäre möglich)
  googleapis_auth: ^2.0.0                 # (War 1.6.0, jetzt auf ^2.0.0)
  googleapis: ^15.0.0                     # (War 13.2.0, jetzt auf ^15.0.0)
  url_launcher: ^6.3.2                    # (War 6.3.1, jetzt auf ^6.3.2)
  image_picker: ^1.1.2                    # (Bleibt, 1.2.1 wäre möglich)
  path_provider: ^2.1.5                   # (Bleibt)
  flutter_secure_storage: ^9.2.4          # (Bleibt)
  excel: ^4.0.6                           # (Bleibt)
  share_plus: ^12.0.1                     # (War 10.1.4, jetzt auf ^12.0.1)
  open_filex: ^4.7.0                      # (Bleibt)
  reorderables: ^0.6.0                    # (War 0.4.2, jetzt auf ^0.6.0)
  dropdown_search: ^5.0.6                 # (War 5.0.6, jetzt auf ^6.0.2)
  collection: ^1.18.0                     # (Bleibt)
  intl: 0.20.2                           # (War 0.19.0, jetzt auf ^0.19.0)
  cupertino_icons: ^1.0.6                 # (Bleibt)
  pdfrx: ^1.1.11                          # (Bleibt)
  flutter_image_converter: ^0.4.1         # (Bleibt)
  photo_view: ^0.15.0                     # (Bleibt)
  flutter_image_compress: ^2.4.0          # (Bleibt)
  web: any                                # (Bleibt)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.3         # (Bleibt)
  flutter_lints: ^6.0.0                   # (Bleibt)
# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/app_icon.jpg"

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/images/Benutzerprofil.png
    - assets/images/Vereinslogo.png
    - assets/images/Vereinssatzung.png
    - assets/images/Beitritterklärung_Hauptverein.png
    - assets/images/Beitragsordnung.png
    - assets/images/Beitritterklärung_Abteilung_Tennis.png
    - assets/images/Natali_Gumbrecht_Trainer.webp
    - assets/images/Oliver_Ströbel_Trainer.webp
    - assets/images/Spielergebnisse.webp
    - assets/images/Dokumentenbox.webp
    - assets/images/Trainer.webp
    - assets/images/Fotogalerie.webp
    - assets/images/Anmelden.png
    - assets/images/Impressum.png
    - assets/images/Admin-Funktionen.png
    - assets/images/Platzbuchung.webp
    - assets/pdfs/Beitrittserklärung_Hauptverein.pdf
    - assets/pdfs/Beitrittserklärung_Abteilung_Tennis.pdf
    - assets/pdfs/Beitragsordnung.pdf
    - assets/pdfs/Vereinssatzung.pdf
    - assets/images/Datenschutz.png
    - assets/images/Getränkebuchung.webp
    - assets/images/Online_shop.webp
    - assets/images/Abmelden.png
    - assets/images/BTV.jpg
    - assets/images/Termine.webp
    - assets/images/ornbau.png
    - assets/service-account.json

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
